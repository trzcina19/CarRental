@model CarRental.Models.Customer
@{
    ViewBag.Title = Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>


    @if (Model.BirthdayDate.HasValue)
    {
    <h4>  Birthday date: @Model.BirthdayDate.Value.ToShortDateString()</h4> 
    }
